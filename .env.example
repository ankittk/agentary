# Optional: data directory (default: ~/.agentary or $AGENTARY_HOME)
# AGENTARY_HOME=/path/to/agentary

# Optional: require X-API-Key or ?api_key= on requests (except /health, /metrics)
# AGENTARY_API_KEY=your-secret-key

# Optional: for manager LLM and/or agent runtimes (subprocess/grpc) that call these providers
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: Slack notifications (if set, capability is registered)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# Optional: GitHub notifications (if both set, capability is registered)
# GITHUB_TOKEN=ghp_...
# GITHUB_OWNER_REPO=owner/repo
